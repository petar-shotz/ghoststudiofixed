rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The browser never talks to Firestore directly. All access goes through
    // validated Next.js server routes using the Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
